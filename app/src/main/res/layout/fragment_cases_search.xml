<?xml version="1.0" encoding="utf-8"?>

<ViewSwitcher xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/view_switcher"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:inAnimation="@anim/slide_in_down"
    android:outAnimation="@anim/slide_out_up">

    <include
        android:id="@+id/case_search_bar_closed"
        layout="@layout/case_search_bar_closed" />

    <include
        android:id="@+id/case_search_bar_opened"
        layout="@layout/case_search_bar_opened" />

</ViewSwitcher>
