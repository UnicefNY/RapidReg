<?xml version="1.0" encoding="utf-8"?>

<ViewSwitcher xmlns:android="http://schemas.android.com/apk/res/android"
              android:id="@+id/view_switcher"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:inAnimation="@anim/slide_in_down"
              android:outAnimation="@anim/slide_out_up">

    <include
        android:id="@+id/record_search_bar_opened"
        layout="@layout/record_search_bar_opened"/>

    <include
        android:id="@+id/record_search_bar_closed"
        layout="@layout/record_search_bar_closed"/>

</ViewSwitcher>
